<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <title>Home Page</title>
</head>
<body>
  <header class="bg-gray-800 text-white p-4 flex justify-between items-center">
    <nav class="flex space-x-4">
      <%= link_to 'Students', students_path, class: 'text-white hover:bg-gray-700 p-2 rounded' %>
      <% if session[:user_id] %>
        <%= link_to 'Sign out', logout_path, class: 'text-white hover:bg-gray-700 p-2 rounded' %>
      <% end %>
    </nav>
    <input type="text" placeholder="Search" class="p-2 rounded bg-gray-700 text-white">
  </header>
  <main class="p-8">
    <section>
      <h2 class="text-2xl mb-4">Info: Most Recent Students</h2>
      <table class="w-full text-left border-collapse">
        <thead>
          <tr>
            <th class="border-b p-2">Name</th>
            <th class="border-b p-2">UIN</th>
          </tr>
        </thead>
        <tbody>
          <!-- Repeat the rows as needed -->
          <tr>
            <td class="border-b p-2">Student Name</td>
            <td class="border-b p-2">12345678</td>
          </tr>
        </tbody>
      </table>
    </section>

    <br>
    <% if current_user.role == 'student_worker' || current_user.role ==  'program_director' || current_user.role == 'super_user' %>
      <section>
        <h2 class="text-2xl mb-4">Student Worker Access</h2>
        <p>Content for student workers.</p>
      </section>
    <% end %>

    <br>
    <% if current_user.role == 'program_director' || current_user.role == 'super_user' %>
      <section>
        <h2 class="text-2xl mb-4">Program Director Access</h2>
        <p>Content for program directors.</p>
      </section>
    <% end %>

    <br>
    <% if current_user.role == 'super_user' %>
      <section>
        <h2 class="text-2xl mb-4">Super User Access</h2>
        <p>Content for super users.</p>
      </section>
    <% end %>

  </main>
</body>
</html>
